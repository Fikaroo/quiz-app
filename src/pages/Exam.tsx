import React from "react";
import { Question } from "../components";

type Props = {};

const Exam = (props: Props) => {
  return (
    <div>
      <Question />
    </div>
  );
};

export default Exam;
